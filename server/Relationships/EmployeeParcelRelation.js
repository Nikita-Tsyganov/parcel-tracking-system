/*const Employee = sequelize.define('../models/Employee.js');
const Parcel = sequelize.define('../models/Parcel.js');

Employee.hasMany(Parcel, {
    foreignKey: 'employee_id'
  });
  Parcel.belongsTo(Employee);
  */